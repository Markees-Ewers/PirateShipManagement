# PirateShipManagement

a mock system where a pirate ship needs to manage their inventory 

to login with as a cremate 
userName is "favCrewMate"
password is "bestCrewMate"

to login as a Quarter Master 
userName is "theQuarter"
password is "masterStockCounter"

a user also has a name field so that is what gets written down when a user changes a stock